1. Personal Task + Habit Tracker

A simple app where you:

Add daily tasks
Mark them complete
Track streaks (for habits)

Core features:

Store tasks in localStorage as JSON
Add/remove/edit tasks
Daily reset (compare dates)
Streak counter per habit

Why it’s good:
You’ll learn CRUD operations, date handling, and UI updates.
